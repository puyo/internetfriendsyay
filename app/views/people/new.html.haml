%ul.breadcrumb
  %li= link_to 'Schedule', @schedule
  %li New person

= form_for [@schedule, @person] do |f|
  = render partial: 'people/form', locals: { person: @person, form: f, available_at_field: 'person[available_at[%d]]' }
  .buttons
    = f.submit
